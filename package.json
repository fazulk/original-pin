{
  "name": "original-pin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/context_menus.js && node --check src/model.js && node --check src/service_worker.js"
  }
}
